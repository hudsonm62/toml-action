{
    "name": "toml-action",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "clean": "rimraf ./dist",
        "build": "npx ncc build ./index.js --license LICENSES.txt --minify",
        "format": "prettier --write ."
    },
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@iarna/toml": "^2.2.5"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.1",
        "prettier": "^3.6.2",
        "rimraf": "^6.0.0"
    }
}
